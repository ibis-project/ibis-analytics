[app]
database="app.ddb"
#database="md:metrics"
[eda]
database="cache.ddb"
#database="md:metrics"
[transform]
database="cache.ddb"
ci_enabled=false
[ci]
#backfill=365
[pypi]
#backfill = 365
packages = [
    "ibis-framework",
]
[github]
repos = [
    "ibis-project/ibis",
    #"ibis-project/ibis-examples",
]
endpoints = [
    "repo",
    "stargazers",
    "subscribers",
    "commits",
    "releases",
    "forks",
    "issues",
    "contributors",
]
[motherduck]
database="metrics"
#exclude_tables = ["penguins"]
exclude_tables = ["penguins", "stars", "issues", "pulls", "forks", "watchers", "commits", "downloads"]
ci_tables = ["stars", "issues", "pulls", "forks", "watchers", "commits"]
